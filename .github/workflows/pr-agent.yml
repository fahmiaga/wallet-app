name: PR Agent Open Source

on:
  pull_request:
    branches:
      - main
    types: [opened, reopened, ready_for_review, synchronize]

jobs:
  pr_agent:
    if: ${{ github.event.sender.type != 'Bot' }}
    runs-on: ubuntu-latest

    permissions:
      issues: write
      pull-requests: write
      contents: write

    steps:
      # - name: Check PR size and add label if large
      #   uses: actions/github-script@v7
      #   with:
      #     script: |
      #       const { data: pr } = await github.rest.pulls.get({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         pull_number: context.payload.pull_request.number
      #       });

      #       const additions = pr.additions;
      #       const deletions = pr.deletions;
      #       const totalChanges = additions + deletions;

      #       console.log(`Total changes: ${totalChanges}`);

      #       const threshold = 500;
      #       const labelName = "Large PR";
      #       const labelColor = "FF0000";

      #       if (totalChanges > threshold) {
      #         // Try to create the label if it doesn't exist
      #         try {
      #           await github.rest.issues.createLabel({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             name: labelName,
      #             color: labelColor
      #           });
      #           console.log("Label 'Large PR' created with red color.");
      #         } catch (error) {
      #           if (error.status === 422) {
      #             // Label already exists, update its color
      #             await github.rest.issues.updateLabel({
      #               owner: context.repo.owner,
      #               repo: context.repo.repo,
      #               name: labelName,
      #               color: labelColor
      #             });
      #             console.log("Label 'Large PR' updated with red color.");
      #           } else {
      #             throw error;
      #           }
      #         }

      #         // Add the label to the PR
      #         await github.rest.issues.addLabels({
      #           owner: context.repo.owner,
      #           repo: context.repo.repo,
      #           issue_number: context.payload.pull_request.number,
      #           labels: [labelName]
      #         });
      #         console.log("Label 'Large PR' added to PR.");
      #       } else {
      #         console.log("PR size is within acceptable limit.");
      #       }

      - name: Run PR Agent
        uses: fahmiaga/qodo@v-debug-1772439754
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          config.model: "gemini/gemini-2.5-pro"
          config.fallback_models: '["gemini/gemini-2.5-flash"]'

          GOOGLE_API_KEY: ${{ secrets.GEMINI_API_KEY }}

          github_action_config.auto_review: "true"
          github_action_config.auto_describe: "false"
          # github_action_config.auto_improve: "true"